// this is for react-slick, which is used for the carousel
window.matchMedia = window.matchMedia || function reactSlickTestHelper() {
  return {
    matches: false,
    addListener() {},
    removeListener() {},
  };
};
